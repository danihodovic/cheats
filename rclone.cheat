% rclone

$ remote: rclone listremotes

# Rclone list remotes
rclone listremotes

# Backup to Google Photos
rclone sync -P Pictures/ googlephotos:album/Screenshots

# Copy a single file via rclone
rclone copy -P my_file.txt <remote>:/opt/files

# Sync a source directory a remote, modifying remote only
rclone sync -P /my_dir <remote>:/my_dir

# add a new remote
rclone config
